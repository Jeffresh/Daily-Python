# Write a program that takes two strings, checks whether the first string contains
# the second one and prints the result of the membership test.
#
# The variables for both input strings have already been defined.

if __name__ == '__main__':
    a = input()
    b = input()
    # run the membership test below
    print(b in a)