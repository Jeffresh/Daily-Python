# Suppose we have a string with some alphabet and we want to store all the letters from it in a tuple.
# Read the input string and print this tuple.

if __name__ == '__main__':
    alphabet = input()
    print(tuple(alphabet))
